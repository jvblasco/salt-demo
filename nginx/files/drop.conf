##################################
# **** DO NOT EDIT THIS FILE ****
# This file is managed by Salt
# Any changes will be overwritten
##################################
location = /app/etc/local.xml { access_log off; log_not_found off; deny all; }
{%- if grains['id'].startswith('balancer') %}
location ~ /\.htaccess        { access_log off; log_not_found off; deny all; }
location ~ /\.ht              { access_log off; log_not_found off; deny all; }
location ~ /\.git             { access_log off; log_not_found off; deny all; }
{%- elif grains['id'].startswith('frontend') %}
location = /robots.txt        { access_log off; log_not_found off; }
location = /favicon.ico       { access_log off; log_not_found off; }
location ~ /\.                { access_log off; log_not_found off; deny all; }
{%- elif grains['id'].startswith('single') %}
location = /robots.txt        { access_log off; log_not_found off; }
location = /favicon.ico       { access_log off; log_not_found off; }
location ~ /\.                { access_log off; log_not_found off; deny all; }
{%- endif %}
location ~ ~$                 { access_log off; log_not_found off; deny all; }
